name: CDPCoreDemoMac
options:
  bundleIdPrefix: com.coinbase
  deploymentTarget:
    macOS: "13.0"
  xcodeVersion: "15.0"

packages:
  CDPCore:
    url: https://github.com/coinbase/cdp-swift
    exactVersion: 0.0.0-alpha.3

targets:
  CDPCoreDemoMac:
    type: application
    platform: macOS
    sources:
      - path: ../CDPCoreDemo/Sources/CDPCoreDemo
        excludes:
          - "Info.plist"
    resources:
      - path: Assets.xcassets
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.coinbase.CDPCoreDemo
        INFOPLIST_FILE: SupportingFiles/Info.plist
        CODE_SIGN_ENTITLEMENTS: SupportingFiles/CDPCoreDemoMac.entitlements
        SWIFT_VERSION: "5.9"
        PRODUCT_NAME: "CDP Wallets Demo"
        MARKETING_VERSION: "1.0"
        CURRENT_PROJECT_VERSION: "1"
    dependencies:
      - package: CDPCore
