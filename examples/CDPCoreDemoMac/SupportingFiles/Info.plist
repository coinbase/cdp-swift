<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>$(DEVELOPMENT_LANGUAGE)</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundleDisplayName</key>
    <string>CDP Wallets Demo</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>LSMinimumSystemVersion</key>
    <string>$(MACOSX_DEPLOYMENT_TARGET)</string>
    <key>NSHumanReadableCopyright</key>
    <string>Copyright © Coinbase. All rights reserved.</string>
    <!--
      NOTE: The `cdpdemo` OAuth callback scheme is intentionally NOT registered
      here. The SDK uses ASWebAuthenticationSession, which registers a transient
      handler for `callbackURLScheme` and captures the redirect itself. Registering
      the same scheme in Info.plist makes macOS LaunchServices route the redirect
      to the app (via .onOpenURL) instead of back to the session, which hangs the
      session's continuation. Apple recommends not registering the scheme when
      using ASWebAuthenticationSession.
    -->
</dict>
</plist>
